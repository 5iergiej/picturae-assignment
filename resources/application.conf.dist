ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.example.ApplicationKt.module ]
    }
    database {
        host = localhost
        port = 5432
        user = user
        password = password
        dbname = dbname
    }
}
